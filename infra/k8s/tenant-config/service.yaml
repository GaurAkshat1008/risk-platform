apiVersion: v1
kind: Service
metadata:
  name: tenant-config
  namespace: risk-platform
spec:
  selector:
    app: tenant-config
  ports:
    - name: grpc
      port: 50052
      targetPort: 50052
    - name: metrics
      port: 9091
      targetPort: 9091
  type: ClusterIP