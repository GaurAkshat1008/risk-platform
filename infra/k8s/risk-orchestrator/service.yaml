apiVersion: v1
kind: Service
metadata:
  name: risk-orchestrator
  namespace: risk-platform
spec:
  selector:
    app: risk-orchestrator
  ports:
    - name: grpc
      port: 50054
      targetPort: 50054
    - name: metrics
      port: 9094
      targetPort: 9094
  type: ClusterIP
