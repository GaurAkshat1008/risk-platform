apiVersion: v1
kind: Service
metadata:
  name: notification
  namespace: risk-platform
spec:
  selector:
    app: notification
  ports:
    - name: grpc
      port: 50061
      targetPort: 50061
    - name: metrics
      port: 9101
      targetPort: 9101
  type: ClusterIP
