query QueryLogs($query: LogQueryInput!) {
  queryLogs(query: $query) {
    entries {
      id
      service
      severity
      message
      traceId
      spanId
      tenantId
      environment
      timestamp
    }
    nextPageToken
  }
}
